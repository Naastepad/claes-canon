# Claude instructions — Claes

This is a thin model-specific entrypoint. Repository discovery and exact raw URLs are centralized in `AI_REPOSITORY_MANIFEST.md`; canonical cross-model operating instructions live in `AI_ONBOARDING.md`.

Before answering canon-sensitive questions or editing this repository, read in this order:

1. `AI_REPOSITORY_MANIFEST.md`
2. `AI_ONBOARDING.md`
3. the ACTIVE operating master identified by the manifest (currently `storybible/LEMMA_MCKEE_MASTER_2026-08-13.md`)
4. all task-relevant modules and structured records identified by the manifest
5. `WRITING_PROTOCOL.md` if drafting/revising literary prose
6. `AUTHORING_POLICY.md`, `AGENTS.md` and `REPOSITORY_INTEGRITY.md` if changing canon, schemas, repository structure or Lemma

If direct repository discovery is unavailable but raw GitHub files can be opened, use this bootstrap URL first:

https://raw.githubusercontent.com/Naastepad/claes-canon/authoring/v1/AI_REPOSITORY_MANIFEST.md

Never infer or fabricate repository paths from neighbouring filenames. Use exact paths/raw URLs from the manifest. If a needed file is not discoverable through the manifest, report that limitation rather than guessing.

Do not substitute conversation memory for repository truth. Preserve the distinction between historical evidence (`SC.*`), novel truth (`STC.*`), narrative instances (`NI.*`) and deterministic Lemma constraints.

Never silently promote `OPEN` or `PROPOSED` material to `CANON`. Never invent precision that the Storybible does not contain.

If writing prose, create or identify the scene contract first: POV, story-time window, active claims, knowledge/object state, opening and closing value, pressure/turn, arcs/relationships/motifs affected, and open decisions that must remain open. Then write the literary text without embedding the metadata labels into the prose.

At the end of substantial work, leave a concise handoff stating records consulted, changes proposed/made, unresolved decisions and validation status.